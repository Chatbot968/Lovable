
export interface ClientConfig {
  client_id: string;
  logo_url: string;
  color_primary: string;
  webhook_url: string;
  bot_description: string;
  banner_color?: string;
  chat_background_color?: string;
  text_color?: string;
  description?: string;
}
